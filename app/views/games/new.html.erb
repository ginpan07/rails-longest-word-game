<div class="container">
  <div class="mb-5">
    <h1>New game 😀
    </h1>
  </div>

  <div>
      <%@letters.each do |letter|%>
      <!--<span class="fw-bold fs-2 bg-primary border w-5 m-2 p-2"><%=letter%></span>-->
      <span class="letter_cards"><%=letter.capitalize%></span>
      <%end%>
  </div>

  <p class="m-3">What is the longest word you can find: </p>
  <form action="/score" method="post" data-turbo="false">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <div class="input-group m-3">
      <input type="text" width="100px" class="form-control" name="word">
      <button type="submit" class="btn btn-primary">Play</button>
    </div>
  </form>

</div>
